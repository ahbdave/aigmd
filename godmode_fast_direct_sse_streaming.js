// GODMODE FAST — Direct SSE streaming
        // Solo: simple stream, no thinking UI, no liquid.
        // ALL 5: stream GODMODE FAST while racing the other 4 in parallel
        //        with thinking bar visible; liquid upgrades can replace.